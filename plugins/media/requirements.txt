# omegaclaw-telegram plugin dependencies not already in core.
# openai is already a core dependency (used for the OpenRouter vision + Whisper calls).
Pillow>=10.0.0
pypdf>=4.0.0
aiogram>=3.0.0
telegramify-markdown>=0.1.0
